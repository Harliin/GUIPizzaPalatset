<UserControl x:Class="GUI_Beställning.Views.PizzaMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI_Beställning.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Background="#FFFFFF"
             d:DesignHeight="450" d:DesignWidth="800">
    <!--FÄRGKODER
    Rosa: #D70F64
    Mörkblå: #314455
    Ljusgrå: #97AABD
    Vit : #FFFFFF
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0" Grid.Row="0">
            <ListView Margin="10" Name="PizzaTable" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding Pizzas}" BorderBrush="#D70F64">
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="3*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*"/>
                                <RowDefinition Height="1*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding Name}" Grid.Column="0" Grid.Row="0" FontSize="30" FontWeight="Bold"/>
                            <TextBlock Text="{Binding ID}" Grid.Column="0" Grid.Row="1" FontSize="25" FontWeight="DemiBold"/>
                            <TextBlock Text="{Binding Price}" Grid.Column="2" Grid.Row="0" Margin="0 0 -10 0"/>
                            <Button Content="{materialDesign:PackIcon Kind=AddCircle, Size=25}" Grid.Column="2" Grid.Row="1" Height="25" Width="25" HorizontalAlignment="Right"/>
                            <GridSplitter Grid.Row="1" FontSize="3" Margin="0 3 0 3"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </DockPanel>
        

    </Grid>
</UserControl>
